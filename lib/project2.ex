defmodule Project2 do

use Application

  def start(_type,_args) do
    # IO.puts "#{inspect args}"
    # {:ok, pid} = Project2.MainSupervisor.start_link()
      # :timer.sleep(1000)



    # NeighbourManager.getNeighbourPIds(1, 3)
    # :timer.sleep(1000)
    # {:ok, pid}
  end
end
